jupyterhub:
  ingress:
    enabled: true
    hosts:
      - eecs.datahub.berkeley.edu
    tls:
      - secretName: tls-cert
        hosts:
          - eecs.datahub.berkeley.edu
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 8Gi
  scheduling:
    userPlaceholder:
      enabled: false
