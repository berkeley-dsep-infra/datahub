jupyterhub-ssh:
  hubUrl: https://prob140.datahub.berkeley.edu

jupyterhub:
  scheduling:
    userPlaceholder:
      enabled: true
      replicas: 10
  ingress:
    enabled: true
    hosts:
      - prob140.datahub.berkeley.edu
    tls:
      - secretName: tls-cert
        hosts:
          - prob140.datahub.berkeley.edu
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 12Gi
