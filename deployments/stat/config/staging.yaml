nfsPVC:
  nfs:
    shareName: shares/stat/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - stat-staging.datahub.berkeley.edu
    tls:
      - secretName: tls-cert
        hosts:
          - stat-staging.datahub.berkeley.edu
