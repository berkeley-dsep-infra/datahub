images:
  image_name: gcr.io/data8x-scratch/data8x-user-image
  registry:
    provider: gcloud
    gcloud:
      project: data8x-scratch
      service_key: gcloud-service-key.json

cluster:
  provider: gcloud
  gcloud:
    project: data8x-scratch
    service_key_path: secrets/gcloud-service-key.json


